import React from 'react';
import { View, Text, ScrollView, StyleSheet, Button} from 'react-native';

const OneMark = (props) => {
    <div data-testid='marker-component'>
        <div className="marktext"> Marker {props.numb}: </div>
    </div>
}
export default OneMark;